#include <string>
#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    //O(n^2)
    vector<long long> naiveApproach(vector<int>& nums) {

    }
    // using prefix sum
    vector<long long> prefixSum(vector<int>& nums) {

        // calculate the first row

        // calculate the first column
        // calculate the whole matrix
    }

    vector<long long> distance(vector<int>& nums) {
        vector<long long> res;

        return res;
    }
};

int main()
{
    Solution sol;
    vector<int> initial = { 1, 3, 1, 1, 2 };
    vector<long long> result = sol.distance(initial);
}